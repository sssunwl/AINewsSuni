# Claude Instructions — AINewsSuni

這個 repo 除了 AI 新聞摘要機器人本體，也是 SS 手機隨手丟連結的「快速收集入口」。

## 個人資源整理（IG / 網頁 / 課程連結收集）

當 SS 貼一個連結給你（IG 貼文、一般網頁文章、線上課程），**很可能連結旁邊會附一段 SS 自己順口打的文字說明（可能很口語、很亂、想到什麼寫什麼）**：

1. **先處理 SS 附的文字說明**：不用逐字照抄，讀懂 SS 想表達的重點，整理成通順、精簡的「重點摘要」。這一步比讀不讀得到原始連結內容更重要——SS 自己的說明往往比原始頁面更直接反映他真正在意的點。

2. **再試著用 WebFetch 讀取連結內容，補充/驗證細節**
   - 一般網頁/課程頁面：通常讀得到，可以補充或修正 SS 的說明
   - Instagram 連結：大多需要登入才能看到內容，WebFetch 會失敗——**這是預期中的限制**，不用一直重試，有 SS 自己的說明文字時影響不大

3. **寫入 Notion 資料庫「IG 收藏整理」——這是唯一真相來源**
   - 資料庫網址：https://app.notion.com/p/b24c50f9af1945d2883ef7356e19094a （若 MCP 有 fetch 工具，用網址或 data source id `collection://64777f8b-6b24-4f1d-ba0f-d46df2ad6c43` 存取）
   - 欄位：標題、連結、分類（目前選項：Claude Code 教學/餐廳/線上課程/文章網頁/8月釜山/其他，之後可能會再增加）、媒體類型（影片/圖片/圖文）、狀態、驗證狀態、平台、課程進度、重點摘要、應用場景、資源介紹、地點
   - **有 SS 的說明文字，或連結讀得到內容**：正常填寫所有欄位，重點摘要以 SS 的說明為主、連結內容為輔，狀態填「已整理」
   - **完全沒有說明文字、連結也讀不到（多半是純丟一個 IG 連結沒多說）**：標題先用網址或當下最合理的猜測，分類先猜測或留給週會確認，**狀態填「待整理」**——不用因為讀不到就拒絕記錄，先讓連結進資料庫

4. **「連結」欄位必須是真實的原始出處網址，這是之後驗證內容真假的唯一依據**：不要放二手連結、不要放錯連結。如果來源內容裡還提到其他有用的連結（例如工具下載頁、GitHub repo、其他相關網站），把這些額外連結整理進「頁面內文」對應段落裡（不要塞進「連結」欄位，那個欄位只放這筆資源本身的原始出處）。

5. **驗證狀態要誠實填，沒有真的核對過來源就不要標「已查證屬實」**：預設填「待驗證」，只有在你真的打開/讀取過「連結」欄位指向的原始來源、確認頁面內文寫的東西跟來源一致時，才能標「已查證屬實」。沒讀到來源內容（例如 IG 讀不到）就老實標「待驗證」或「不適用」，不要因為想讓資料看起來完整就亂標。

6. **輸出永遠用繁體中文，不管來源是什麼語言**：來源是簡體中文、英文、日文或其他語言都一樣，你要做的是**真正讀懂內容後，用繁體中文重新寫一遍**（像雙語使用者會做的事），而不是用機械式的簡繁轉換工具去跑一次。機械轉換常常會出現同音异字/選字錯誤，寫出來的東西會變成看起來像中文但語意不通的亂碼（曾經真實發生過，教訓慘痛）。如果對某個詞不確定繁體怎麼寫，寧可用你自己的話換句話說，也不要硬套一個可能是錯的字。

7. **成功寫入 Notion 後，在 `RESOURCES.md` 的索引表加一行**：`RESOURCES.md` 只是**輕量索引**（標題/分類/一行重點/狀態/Notion連結），不是完整內容鏡像——完整內容永遠只在 Notion 看。每寫入一筆 Notion，就在 RESOURCES.md 的索引表加一行對應資料並 commit。

8. **如果 Notion 寫入失敗（網路問題、rate limit 等真正的錯誤，不是分類選項不存在——那個直接用「其他」解決）**：不要放棄、也不要讓資料憑空消失。把完整資料（含原本要寫的所有欄位跟頁面內文內容）寫進 `RESOURCES.md` 的「待同步到 Notion（暫存區）」區塊，commit 起來當暫存備份。等下次能正常寫入 Notion 時（可能是同一個 session 重試，也可能是下次開新 session、或交給週會處理），把暫存內容補寫進 Notion，成功後從暫存區刪除、移到上面的索引表。

9. **分類欄位找不到合適選項時，直接用「其他」，不要自己新增分類、也不要因此卡住或放棄 Notion**：資源會越來越多，等累積到一定量，SS 會定期跟 Sun 一起 review、決定要不要新增正式分類並重新分類。**你（雲端 session）不要自己嘗試新增分類選項**——Notion 的下拉選單型欄位只接受已存在的值，硬寫一個不存在的新值會直接被 API 拒絕（這不是權限問題），先用「其他」頂著就對了，不用糾結。

10. **每一筆資源都要寫「頁面內文」，不是只填欄位**：光填「重點摘要」「資源介紹」等欄位不夠完整，還要在頁面本體（content，用 insert_content 或 replace_content）寫一段有結構的介紹，讓人點進 Notion 頁面就能看懂全貌。內容深度依資源類型調整，沒有固定模板，但至少要有 2-3 個段落標題（## 開頭），例如：
    - **工具/專案類**：項目簡介、核心功能、技術規格（語言/授權/依賴）、適合對象
    - **餐廳類**：推薦理由、招牌與細節、環境、交通位置、適合場合
    - **課程/文章類**：核心內容、重點筆記、適合對象
    參考範例：Agent Sprite Forge 這筆（已有完整內文可參考格式）。

11. **不用勉強做超出能力範圍的事**：雲端 session 沒有 Claude in Chrome，讀不到需要登入的內容是正常現象，如實記錄即可，不要編造內容或猜測摘要當真的寫進去。但「Notion 寫入失敗」跟「讀不到網頁內容」是兩回事，遇到分類問題直接用「其他」解決，遇到真正的寫入失敗就用暫存區，不要整個放棄 Notion 另建平行系統。

## 每週 Review 機制

SS 每週會跟 Sun（本機、有 Claude in Chrome 的 session）討論一次，把「狀態＝待整理」的項目（主要是沒附說明文字又讀不到的 IG 連結）逐一打開、寫摘要、改成「已整理」，並視需要做「彙整」（例如某月餐廳推薦、某主題教學總結）。同時檢查 `RESOURCES.md` 的暫存區有沒有卡著的項目，補寫進 Notion。

**Sun 也可以自發性整理，不用每次都等 SS 開口**：發現資料庫累積一定數量的「已整理」項目時，可以主動提議做彙整；發現「待整理」堆積太多也可以主動提醒，不需要每次都被動等指令。

## 其他

本 repo 也是 AINewsSuni（AI 新聞摘要機器人）本體所在，詳見 [README.md](README.md)；AI 學院教學素材在 [ai-academy/](ai-academy/README.md)。
